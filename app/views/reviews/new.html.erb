<h1>Add Review for <%= @restaurant.name %></h1>

<%= simple_form_for [@restaurant, @review] do |f| %>
  <%= f.input :content %>
  <%= f.input :rating, as: :radio_buttons, collection: [0, 1, 2, 3, 4, 5] %>
  <%= f.submit "Submit Review", class: "btn btn-success" %>
<% end %>
